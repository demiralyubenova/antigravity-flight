[phases.setup]
nixPkgs = ["nodejs", "python311", "python311Packages.pip"]

[phases.install]
cmds = [
  "npm ci",
  "pip install -r python/requirements.txt"
]

[start]
cmd = "tsx server.ts & uvicorn python.main:app --host 0.0.0.0 --port 8000"
