[phases.setup]
nixPkgs = ["nodejs", "python311", "python311Packages.pip"]

[phases.install]
cmds = [
  "npm ci",
  "npm run build",
  "python3 -m venv python/.venv",
  "python/.venv/bin/pip install rembg[cpu] pillow pillow-heif"
]

[start]
cmd = "node dist/server.js"
